# Roden Law — local-SEO pipeline: handoff

Pick this up in a fresh Claude Code session rooted **here** (`Blue Sky Studio/Roden Law/local-seo-pipeline/`) — cwd-walk then resolves Roden's config automatically.

## Goal

Finish wiring Roden Law into the local-SEO pipeline so `/local-seo-cycle` runs a full cycle for Roden.

## Context (also in memory)

Read these first — they have the full picture:
- Memory: `project_local_seo_pipeline.md`, `project_roden_law_content_system.md`
- The skill (authoritative runbook): `~/.claude/skills/local-seo-cycle/SKILL.md`
- Roden writer agent: `~/.claude/agents/roden-content-writer.md`

## What's already done & verified

- **Pipeline code** is centralized in `~/Code/blue-sky-studio/internal-ai-scripts/scripts/local-seo/` (`npm install` already run there). Clients supply only config + data.
- **This folder** is Roden's client dir: `local-seo.config.json` (6 offices, `publish.target: wordpress-ssh`, `writer.agent: roden-content-writer`), `.env.local` (Local Falcon + OpenAI keys, Slack `C0B3W5D41V0`), empty `data/local-seo/`.
- **SSH + wp-cli** works: `rodenlawprod` (prod) + `rodenlawdev1` (staging) via `~/.ssh/wpengine_ed25519`. Read path verified (721 live items). **Write path verified on staging** — created + deleted a draft.
- **`lib/wordpress.mjs`** built + tested: `listWpContent()`, `generateBlogSeeder()`, `publishBlogPost()` (creates a DRAFT), `setFeaturedImage()`, `wpHealthcheck()`. Seeder modeled on the theme's `inc/seed-ebike-pillar.php`.

## What's left — the actual work

All code edits land in **`internal-ai-scripts`** (PR there), not this folder.

1. **Wire Stages 2/4/5/6 to branch on `config.publish.target`** (`publishTarget(cfg)` helper exists in `lib/config.mjs`):
   - **Stage 2 (`research.mjs`)** — for `wordpress-ssh`, internal-link cross-referencing must use `listWpContent()` from `lib/wordpress.mjs`, not the file-based `buildInventory()` (Roden has no `content/` dir).
   - **Stage 4 (`image.mjs`)** — for `wordpress-ssh`, after Stage 5 creates the post, attach the image with `setFeaturedImage()` instead of saving to `public/images/posts/`.
   - **Stage 5** — for `wordpress-ssh`, publish via `publishBlogPost()` (SSH seeder → draft) instead of git commit/PR. **Test on `rodenlawdev1` staging first.** Note the seam: `roden-content-writer` emits markdown + a frontmatter block; `generateBlogSeeder()` wants `{slug, title, contentHtml, excerpt, jurisdiction, authorAttorneySlug, categorySlug, categoryName}` — Stage 5 (or the skill) converts the agent output into that shape.
   - **Stage 6** — poll the WP post URL after a human publishes the draft; Postplanify part is unchanged.
2. **Build Roden's `data/local-seo/keyword-universe.json`** (Stage 0) — run `keyword-agent.mjs` from this folder, or hand-build. Roden's 22 practice areas + 5-metro geography (Savannah/Darien GA, Charleston/N.Charleston/Columbia/Myrtle Beach SC).

## Key facts for the wiring

- 6 offices, each pairs an LF place_id ↔ Postplanify GBP — all in `local-seo.config.json`. 3 LF locations aren't group-tagged; `resolveFalconLocation()` already falls back past the group filter.
- Jurisdiction: GA = 2yr SOL O.C.G.A. § 9-3-33, comparative fault <50% § 51-12-33; SC = 3yr S.C. Code § 15-3-530, <51%. Pillars show both; intersection pages single-state.
- WP CPTs: `practice_area` (live), `post` (blog). The pipeline produces blog `post`s. Posts created as **drafts** — a human publishes in WP admin.
- North Charleston office NAP is unverified — flagged in the config and the writer agent.

## How to verify as you go

```bash
cd "Blue Sky Studio/Roden Law/local-seo-pipeline"   # this folder
PIPE=~/Code/blue-sky-studio/internal-ai-scripts/scripts/local-seo
node --env-file=.env.local "$PIPE/scan.mjs" --dry-run --location Savannah
```

Slack updates go to `#roden-local-seo-pipeline` (`C0B3W5D41V0`).
