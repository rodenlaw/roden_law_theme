// GENERATED by internal-ai-scripts/scripts/site-health/vendor.mjs from scripts/site-health/lib/args.mjs (source sha256 faa164d8600b). Do not edit; re-run vendor.mjs.
// Minimal argv parser. `booleans` names flags that never take a value, so
// `--strict crawl` leaves `crawl` as a positional check name.
export function parseArgs(argv, { booleans = [] } = {}) {
  const bool = new Set(booleans);
  const args = { _: [] };
  for (let i = 0; i < argv.length; i++) {
    const a = argv[i];
    if (!a.startsWith('--')) { args._.push(a); continue; }
    const eq = a.indexOf('=');
    if (eq > 0) { args[a.slice(2, eq)] = a.slice(eq + 1); continue; }
    const name = a.slice(2);
    if (bool.has(name) || i + 1 >= argv.length || argv[i + 1].startsWith('--')) args[name] = true;
    else args[name] = argv[++i];
  }
  return args;
}
